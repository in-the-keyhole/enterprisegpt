// Dependencies
import './DotLoader.css';

/**
 * Inline loading dots loader. Mainly used for loading states that require a
 * smaller inline loader.
 *
 * @export
 * @returns {JSX.Element}
 */
export default function DotLoader(): JSX.Element {
    return <div className="loading-dots"></div>;
}
