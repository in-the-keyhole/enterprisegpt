/**
 * No match page handles the display of the application not resolving the
 * browser route (404).
 *
 * @returns {JSX.Element}
 */
export default function NoMatchPage(): JSX.Element {
    return (
        <div>
            <h3>No Page Found</h3>
        </div>
    );
}
